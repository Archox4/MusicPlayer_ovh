<Window x:Class="MusicPlayer_ovh.DownloaderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicPlayer_ovh"
        xmlns:fa="http://schemas.fontawesome.com/icons/" xmlns:fa1="http://schemas.fontawesome.io/icons/"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="DownloaderWindow" Height="650" Width="800" Background="#FF171717" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="75" />
            <RowDefinition Height="75" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="btPath" Grid.Column="1" Grid.Row="0" Style="{StaticResource PathButtonStyle}" Content="Click to copy path" HorizontalAlignment="Left" VerticalAlignment="Center" Height="40" Width="450" Click="btPathClick"/>
            <Button Grid.Column="1" Grid.Row="0" Width="80" Style="{StaticResource PathButtonStyleOpenExplorer}" Height="40" Click="DownloadButton_Click">
                <fa1:ImageAwesome Icon="Download" Width="20" Height="20" Foreground="Black"/>
            </Button>
        </StackPanel>
        <Grid Grid.Row="1" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=False}">
            <ProgressBar  IsIndeterminate="True" Style="{StaticResource MaterialDesignCircularProgressBar}" x:Name="progressBar" HorizontalAlignment="Left" Height="40" Width="40" VerticalAlignment="Center"  Maximum="1" Background="#7F000000" Foreground="#FF1DB954" Margin="18,0,0,0"/>
            <Label x:Name="downloadLabel" Content="Downloading: " FontSize="16" HorizontalAlignment="Left" VerticalAlignment="Center" Height="30" Width="706" Foreground="#FFE0E0E0" Margin="70,0,0,0"/>
        </Grid>

    </Grid>
</Window>
